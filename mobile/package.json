{
  "name": "ghost-tunnel-mobile",
  "version": "1.0.0",
  "description": "GhostTunnel — Android VPN client",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:build": "cap build android",
    "cap:open": "cap open android",
    "cap:run": "cap run android",
    "build:apk": "npm run build && npm run cap:sync && npm run cap:build"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "capacitor-wireguard": "file:./plugins/capacitor-wireguard",
    "qrcodejs": "^1.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
